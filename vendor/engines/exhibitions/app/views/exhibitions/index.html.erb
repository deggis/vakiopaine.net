<h3><%= @page.title %></h3>

<%=raw @page.content_for(:body)%>

<hr />
<p>Näytetään <strong>menossa tai tulossa olevat</strong> näyttelyt. Arkisto löytyy
<%= link_to "täältä", "kaikki/" %>.
</p>
<hr />

<ul class="no_indent">
<% @upcoming_exhibitions.each do |exhibition| %>
  <li>
    <%= render exhibition %>
  </li>
<% end %>
</ul>
