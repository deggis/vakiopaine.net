<h3><%= @page.title %> </h3>

<%=raw @page.content_for(:body) %>

<hr />

<p>Näytetään vain <strong>meneillään olevat</strong> tai <strong>tulevat tapahtumat</strong>. Menneetkin tapahtumat sisältävän listan löydät
<%= link_to "täältä", "kaikki/" %>.
</p>

<hr />

<ul class="no_indent">
<% @upcoming_events.each do |event| %>
  <li>
    <%= render event %>
  </li>
<% end %>
